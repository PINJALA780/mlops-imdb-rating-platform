apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: imdb-api-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: imdb-api
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
